/** Injected by vite.web.config.ts's `define` (the release version, e.g. "1.1.2") — used to pin the web build's jsDelivr icon-CDN URLs to an immutable tag. Unused/unset in the Electron build. */
declare const __APP_VERSION__: string;
